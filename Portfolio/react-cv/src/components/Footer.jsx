import React from 'react';

function Footer() {
  // original site did not have an explicit global footer outside pages.
  // returning null to maintain 100% visual parity with original structure.
  return null;
}

export default Footer;
